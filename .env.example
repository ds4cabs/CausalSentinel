# Template file (safe to commit to GitHub) - lists the keys the project needs, no real values.
# Usage: put the real keys in ../.env (one level above the repo; never committed).

GEMINI_API_KEY=your-gemini-key-here
GOOGLE_API_KEY=your-gemini-key-here
YOU_API_KEY=your-you-dot-com-key-here
SEMANTIC_SCHOLAR_API_KEY=your-semantic-scholar-key-here
